import React from "react";

const CardView =({data})=>{
    // const headings = Object.keys(data[0])
    return(
        <>
        card view
            {/* <table>
                <thead>
                    {headings.map((heading)=>(
                        <th>{heading}</th>
                    ))}
                </thead>
                <tbody>
                    {data.map((item)=>(
                        <td>{item}</td>
                    ))}
                </tbody>
            </table> */}
        </>
    )
}

export default CardView